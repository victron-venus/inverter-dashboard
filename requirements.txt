# Remote Web Server Dependencies
# Exact versions pinned (kept in sync with uv.lock) for reproducible PyInstaller builds.
fastapi==0.141.1
uvicorn==0.52.3
websockets==17.0.1
paho-mqtt==2.1.0
msgpack==1.2.1
httpx==0.28.1
